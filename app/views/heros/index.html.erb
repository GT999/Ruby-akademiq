<h1>Listing heros</h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <!--<th>Password</th>-->
      <!--<th>Password confirmation</th>-->
      <th>Game</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @heros.each do |hero| %>
      <tr>
        <td><%= hero.name %></td>
        <!--<td><%= hero.password %></td>-->
        <!--<td><%= hero.password_confirmation %></td>-->
        <td><%= hero.game_id %></td>
        <td><%= link_to 'Show', hero %></td>
        <td><%= link_to 'Edit', edit_hero_path(hero) %></td>
        <td><%= link_to 'Destroy', hero, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Hero', new_hero_path %>
